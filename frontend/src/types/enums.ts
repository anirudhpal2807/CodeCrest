// Enums for the LeetCode clone homepage
export enum ProblemDifficulty {
  EASY = 'easy',
  MEDIUM = 'medium', 
  HARD = 'hard'
}

export enum ProblemTag {
  ARRAY = 'array',
  LINKED_LIST = 'linkedList',
  GRAPH = 'graph',
  DP = 'dp'
}

export enum FilterStatus {
  ALL = 'all',
  SOLVED = 'solved'
}

export enum UserRole {
  USER = 'user',
  ADMIN = 'admin'
}